*.iml
.gradle
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
app/.cxx
app/release

# Rust
/solo-conversation-core/target

# Vendored native/AAR dependencies fetched per app/libs/README.md -- not committed,
# same reasoning as the Whisper/NLLB .onnx files (large binary, fetched at build/setup
# time rather than checked in).
app/libs/*.aar
app/libs/*.jar
